# @forge/daemon

Daemon local persistente (MVP).

Incluye:

- status enriquecido (`/status`)
- restore rápido de sesión (`/sessions/latest`, `/sessions/latest/resume`)
- stream SSE de eventos (`/events/stream`)
